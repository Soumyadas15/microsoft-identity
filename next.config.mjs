/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "s.gravatar.com",
            "apis.live.net"
        ]
    },
};

export default nextConfig;
